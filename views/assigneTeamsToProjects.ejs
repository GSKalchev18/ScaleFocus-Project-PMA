<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/users_table.css">
    <link rel="stylesheet" href="css/form_style.css">
    <title>Users list</title>
</head>
<body>

    <%- include('partials/header') %>

    <h1 class = "UserListLable">Teams list</h1>
    <div class="table-wrapper">
        <table class="fl-table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                </tr>
            </thead>
            <tbody>
                <%for(let i = 0; i < TeamsList.length; i++) { %>
                    <tr>
                        <td><%=TeamsList[i].Id%></td>
                        <td><%=TeamsList[i].Title%></td>
                    </tr>
                <%}%>
            </tbody>

        </table>
    </div>
    <h1 class = "UserListLable">Projects list</h1>
    <div class="table-wrapper">
        <table class="fl-table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                </tr>
            </thead>
            <tbody>
                <%for(let i = 0; i < ProjectsList.length; i++) { %>
                    <tr>
                        <td><%=ProjectsList[i].Id%></td>
                        <td><%=ProjectsList[i].Title%>%></td>
                    </tr>
                <%}%>
            </tbody>

        </table>
    </div>

    <div class="form-container">
        <div class="login">
            <form name = "registration-form" class="contact-form" method="POST" action="/teamsprojects">
                <h2 class = "UserListLable">Assign Team</h2>
                <input type="number" name="TeamId" id="TeamId" placeholder="Team Id" required>
                <input type="number" name="ProjectId" id="ProjectId" placeholder="Project Id" required>
                <input class="submit" type="submit" value = "Assign">
            </form>
        </div>
    </div>
    
</body>
</html>